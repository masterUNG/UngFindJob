import 'package:flutter/material.dart';

class MyStyle {
  // Field
  Color primaryColor = Color.fromARGB(0xff, 0x55, 0x8b, 0x2f);
  Color darkColor = Color.fromARGB(0xff, 0x25, 0x5d, 0x00);

  // Method

  SizedBox mySizeBox() => SizedBox(
        height: 16.0,
      );

  MyStyle();
}
